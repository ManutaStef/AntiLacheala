version: '3.8'

services:
  # Serviciul Orchestrator (Poarta de intrare publică)
  orchestrator-service:
    build: .
    ports:
      - "8080:8080"
    environment:
      - SERVER_PORT=8080
    depends_on:
      - agenda-service
      - log-service

  # Serviciul care stochează datele agendei
  agenda-service:
    build: .
    environment:
      - SERVER_PORT=8081
    ports:
      - "8081:8081"

  # Serviciul responsabil exclusiv de afișarea logurilor formatate
  log-service:
    build: .
    environment:
      - SERVER_PORT=8082
    ports:
      - "8082:8082"